# ============================================================
# RistoSaaS — Root env reference
# Copia questo file in `.env` nella root del monorepo.
# I valori applicativi completi stanno in `apps/web/.env.example`.
# ============================================================

# ---------- Database / PostgreSQL ----------
DATABASE_URL=postgresql://user:password@host:5432/ristosaas?schema=public

# ---------- Auth / JWT ----------
JWT_SECRET=replace-with-a-long-random-secret
REFRESH_MAX_AGE_SECONDS=1209600

# ---------- App URL ----------
NEXT_PUBLIC_APP_URL=http://localhost:3000