# Solo DATABASE_URL del database PostgreSQL STAGING (Railway).
# Copia come apps/web/.env.staging e inserisci l'URL fornito.
# NON committare .env.staging (contiene credenziali).

DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/railway
